import React from "react";
import { Root, Trigger } from "@radix-ui/react-select";

const SelectBox = () => {
  return (
    <Root>
      <Trigger></Trigger>
    </Root>
  );
};

export default SelectBox;
